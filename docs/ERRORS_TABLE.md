# Erros Comuns e Soluções — Assistente SISCAN RPA
<a name="errors"></a>

Versão: 2.1
Data: 2026-05-26

Os erros estão organizados em quatro grupos:
- **Incidentes observados em campo** — erros reais capturados na jornada de deploy do servidor parceiro (13/03 → 26/05/2026), ordenados cronologicamente.
- **Comuns** — ocorrem em qualquer modo (HOST ou Servidor).
- **Específicos — Modo HOST** — exclusivos de Windows / Docker Desktop / WSL2.
- **Específicos — Modo Servidor** — exclusivos de Ubuntu / PostgreSQL externo / runner.

Para diagnóstico passo a passo de cada categoria, consulte o [TROUBLESHOOTING.md](TROUBLESHOOTING.md).

> **Novidade na v2.1**: nova seção *Incidentes observados em campo* consolida 34 incidentes reais
> (22 em março, 4 em abril, 8 em maio) reportados no registro interno pela equipe de operação
> (equipe de operação) durante a jornada de deploy nas VMs `<HOST-RPA>`
> e `<HOST-DASHBOARD>`. Cada incidente está rastreado de volta ao turno correspondente
> na timeline consolidada do
> [prisma-ccm-docs#185](registro interno).

---

## Incidentes observados em campo (13/03 → 26/05/2026)

Erros reais capturados durante a instalação e operação do `siscan-rpa` e `siscan-dashboard`
no servidor parceiro. Contexto narrativo completo na timeline
consolidada em [prisma-ccm-docs#185](registro interno).

A coluna **Cobertura atual** indica qual specialist do `siscan-server-doctor.sh` hoje detecta
o sintoma (ou `manual` quando o passo ainda exige inspeção humana).

| # | Data / Turno | Sintoma | Mensagem literal | Causa raiz | Solução aplicada | Cobertura atual | Ref. timeline |
|---:|---|---|---|---|---|---|---|
| F01 | 19/03/2026 Manhã | `git clone` do assistente falha no servidor parceiro com handshake TLS interrompido | `fatal: unable to access 'https://github.com/Prisma-Consultoria/assistente-siscan-rpa.git/': gnutls_handshake() failed: The TLS connection was non-properly terminated.` | Firewall do servidor parceiro bloqueando saída para `github.com:443` antes da regra de whitelist ter sido aplicada | Validação com `curl -Iv https://github.com` (mesmo `SSL_ERROR_SYSCALL`) → requisição interna pra liberação por FQDN; após whitelist, `git clone` repetido com sucesso | `check-network` (endpoints `github.com`, `api.github.com`, `codeload.github.com`) | [Turno 19/03 Manhã](registro interno) |
| F02 | 19/03/2026 Manhã | `curl` direto pra `github.com` confirma bloqueio antes do TLS subir | `* OpenSSL SSL_connect: SSL_ERROR_SYSCALL in connection to github.com:443`<br>`* Closing connection`<br>`curl: (35) OpenSSL SSL_connect: SSL_ERROR_SYSCALL in connection to github.com:443` | Mesma causa raiz de F01 — firewall interrompe a conexão após o Client Hello | Diagnóstico: ausência de resposta TCP/TLS apesar do TCP estabelecer = bloqueio L7 (DPI/inspection). Pedido formal de liberação por FQDN | `check-network` (categoria `Runner ↔️ GitHub Actions`) | [Turno 19/03 Manhã](registro interno) |
| F03 | 19/03/2026 Manhã | Runner GitHub recusa rodar como root | `Must not run with sudo`<br>`ERRO: Falha ao registrar o runner. Verifique a URL e o token (tokens expiram após alguns minutos).` | Restrição de segurança do próprio binário `config.sh` do GitHub Actions runner — proíbe execução como root para não dar superpoderes a workflows arbitrários | `sudo useradd -m -s /bin/bash siscan && sudo usermod -aG docker siscan && sudo su - siscan` antes de re-tentar o registro | `check-docker` (valida `user atual` é não-root); `check-runner` (instalação local) | [Turno 19/03 Manhã](registro interno) |
| F04 | 19/03/2026 Tarde | Pool default Docker `/24` esgotado ao criar rede da stack | `docker network inspect` retorna apenas `"Subnet": "192.168.5.0/24"` ocupada; criação de nova rede falha por sub-rede indisponível | `daemon.json` configurado pelo time do servidor parceiro com pool único `192.168.4.0/24 size 24` (uma única `/24`) para evitar conflito com a rede interna 192.168.x.x | Expandir o pool no `/etc/docker/daemon.json` para `{"base": "172.16.0.0/12", "size": 24}` e reiniciar Docker | `check-docker` (categoria *Pool de redes (daemon.json)* + teste real de `docker network create/rm`) — corresponde ao Problema 1 do TROUBLESHOOTING Servidor | [Turno 19/03 Tarde](registro interno) |
| F05 | 19/03/2026 Tarde | `git pull` do clone do assistente falha por dono diferente do usuário corrente | `fatal: detected dubious ownership in repository at '/app/assistente-siscan-rpa'`<br>`To add an exception for this directory, call:`<br>`        git config --global --add safe.directory /app/assistente-siscan-rpa` | Diretório foi clonado inicialmente como `root` (antes do usuário `siscan` existir), o git 2.35+ rejeita `git pull` por outro usuário | `sudo chown -R siscan:siscan /app/assistente-siscan-rpa` (ou `git config --global --add safe.directory ...`) | `check-permissions` (categorias *Stack dir* e *Git safe.directory*) — corresponde ao Problema 9 do TROUBLESHOOTING | [Turno 19/03 Tarde](registro interno) |
| F06 | 19/03/2026 Tarde | `git pull` ainda falha mesmo após `safe.directory` por permissão de escrita | `error: cannot open '.git/FETCH_HEAD': Permission denied` | Ownership do diretório `.git/` ainda pertence ao usuário original (root); `safe.directory` resolve só a leitura | Mesmo `chown -R siscan:siscan` da F05 — endereça leitura **e** escrita ao mesmo tempo | `check-permissions` (mesma cobertura de F05) | [Turno 19/03 Tarde](registro interno) |
| F07 | 19/03/2026 Tarde | Stack atualizada via `git pull` no clone do assistente, mas `compose` continua usando arquivo antigo | (sem mensagem de erro — sintoma: nova `subnet` do compose não tem efeito após `docker compose up`) | Script rodava `docker compose` em `$HOME/assistente-siscan-rpa`, mas a stack vivia em `/app/siscan-rpa` (`COMPOSE_DIR`). O `git pull` atualizava o compose no clone do assistente, não no diretório onde a stack era executada | Ajuste no workflow CD: copiar `docker-compose.prd.external-db.yml` pro `$COMPOSE_DIR` real + `sudo chown -R siscan:siscan /app/siscan-rpa`. Depois consolidado removendo `COMPOSE_DIR` (stack passa a viver no clone) | `check-stack` (valida que compose file existe no `COMPOSE_DIR` declarado) | [Turno 19/03 Tarde](registro interno) |
| F08 | 20/03/2026 Tarde | Containers da stack entram em `Restarting (n)` loop logo após `compose up` | `siscan-rpa-app-1 ... Restarting (3) 34 seconds ago`<br>`siscan-rpa-rpa-scheduler-1 ... Restarting (1) 9 seconds ago` (repetido a cada poll) | (sintoma — causa raiz revelada em F09) | Inspecionar `docker logs siscan-rpa-app-1 --tail=30` em vez de assumir healthcheck/banco | `check-stack` (categoria *Saúde dos containers — restart loop*) | [Turno 20/03 Tarde](registro interno) |
| F09 | 20/03/2026 Tarde | Workers Gunicorn falham no boot por dependência Python ausente | `assert jinja2 is not None, "jinja2 must be installed to use Jinja2Templates"`<br>`AssertionError: jinja2 must be installed to use Jinja2Templates`<br>`[2026-03-20 17:28:31 -0300] [1] [ERROR] Shutting down: Master`<br>`[2026-03-20 17:28:31 -0300] [1] [ERROR] Reason: Worker failed to boot.` | `jinja2` ausente da imagem `ghcr.io/prisma-consultoria/siscan-rpa-rpa:main` — `src/admin/routes.py` importa `Jinja2Templates` do Starlette sem a dep declarada | PR #478 no `siscan-rpa`: adiciona `jinja2` ao requirements; deploy automático publica nova imagem; restart limpo no servidor | `check-stack` (categoria *Saúde dos containers — restart loop*) detecta o sintoma; correção de imagem fica no CI do `siscan-rpa` | [Turno 20/03 Tarde](registro interno) |
| F10 | 20/03/2026 Tarde | `svc.sh stop/start` falha quando rodado de fora do diretório do runner | `[sudo] password for siscan:`<br>`Failed: Must run from runner root or install is corrupt` | `svc.sh` exige `pwd` apontando pra raiz do runner (usa `./bin/actions.runner.service.template`); script foi invocado via `~/actions-runner/svc.sh` (path absoluto, sem `cd`) | `cd ~/actions-runner && sudo ./svc.sh stop && sudo ./svc.sh start` | `check-runner` (validação do runtime do runner) — passo manual coberto pelo `siscan-runner-recover.sh` | [Turno 20/03 Tarde](registro interno) |
| F11 | 20/03/2026 Tarde | Workflow CD executa mas usa caminho antigo da stack porque `DIR_SISCAN_ASSISTENTE` não chega no shell do runner | `cat ~/actions-runner/.env` retorna só `LANG=C.UTF-8` (variável `DIR_SISCAN_ASSISTENTE` perdida entre stop/start) | Variável foi exportada via `~/.bashrc` em vez de `~/actions-runner/.env` — `runsvc.sh` só carrega o `.env` do diretório do runner, não o bashrc do usuário | `echo 'DIR_SISCAN_ASSISTENTE=/app/assistente-siscan-rpa' >> ~/actions-runner/.env` + reiniciar serviço; depois consolidado removendo `COMPOSE_DIR` do script (issue #17) | `check-env` (variáveis obrigatórias do produto) — o setup do runner agora persiste corretamente via `siscan-server-setup.sh` | [Turno 20/03 Tarde](registro interno) |
| F12 | 20/03/2026 Noite | `cli.py create-user admin --admin` falha com confirmação de senha mal-pareada e depois constata duplicidade | `Password:`<br>`Repeat for confirmation:`<br>`Error: The two entered values do not match.`<br>`Password:`<br>`Repeat for confirmation:`<br>`Erro: usuário 'admin' já existe.` | Usuário admin já criado por execução anterior; mensagem ambígua sugere problema de digitação | Usar `set-user-password` em vez de `create-user`; documentar no roteiro de bootstrap do servidor | manual (ação CLI da aplicação `siscan-rpa`) | [Turno 20/03 Noite](registro interno) |
| F13 | 25/03/2026 Manhã | RPA não consegue acessar SISCAN portal porque credenciais não foram cadastradas no admin | `RuntimeError: Credenciais do SISCAN não configuradas.`<br>`Cadastre no admin ou defina SISCAN_USER/SISCAN_PASSWORD no ambiente.` | Stack subiu, mas a etapa pós-deploy de cadastrar credenciais SISCAN em `/admin/siscan-credentials` foi pulada (não estava nos passos automatizados) | Acessar `http://<IP-VM1>:5001/admin/siscan-credentials` e cadastrar credenciais reais do portal SISCAN. Passou a ser pré-requisito documentado na issue #22 | manual (etapa de bootstrap operacional do `siscan-rpa`) | [Turno 25/03 Manhã](registro interno) |
| F14 | 25/03/2026 Tarde | Após nginx reverse proxy adicionado, login no admin falha como se "senha tivesse expirado" | (sem mensagem literal — sintoma: "Não consigo entrar mais com nenhuma senha" reportado pela equipe; senhas cadastradas no mesmo dia já não funcionam) | (causa raiz só identificada em F18 — chaves RSA regeneradas a cada deploy invalidam hashes do banco) | Workaround imediato: criar novos usuários após cada deploy. Cura definitiva em F18 | `check-permissions` futuro (cobertura de `HOST_SECRETS_DIR`/RSA keys mencionada na issue #43) | [Turno 25/03 Tarde](registro interno) |
| F15 | 26/03/2026 Manhã | Geração de token do runner do `siscan-dashboard` retorna 404 na página GitHub | (screenshot — 404 na rota `Settings → Actions → Runners → New self-hosted runner` do repo `siscan-dashboard`) | Repositório `siscan-dashboard` ainda não tinha Actions habilitado ou usuário sem permissão pra gerar token nesse repo (apenas no `siscan-rpa`) | Garantir permissão de admin no repo `siscan-dashboard`; gerar token diretamente; documentar geração de token por repo no roteiro | manual (passo no GitHub UI) | [Turno 26/03 Manhã](registro interno) |
| F16 | 27/03/2026 Manhã | Registro do runner do dashboard via API falha por TLS interrompido | `What is the URL of your repository? https://github.com/Prisma-Consultoria/siscan-dashboard`<br>`What is your runner register token? *****************************`<br>`The SSL connection could not be established, see inner exception.` | Mesma raiz de F01/F02 — firewall do servidor parceiro reabriu para `github.com` mas algum endpoint específico do registro (provavelmente `api.github.com`) ainda bloqueado intermitentemente | Diagnóstico com `curl -Iv https://github.com` (re-bate `SSL_ERROR_SYSCALL`) → retry após confirmar regra de whitelist; depois solução durável via `siscan-runner-recover.sh` | `check-network` (`api.github.com`, `pipelines.actions.githubusercontent.com`) | [Turno 27/03 Manhã](registro interno) |
| F17 | 27/03/2026 Manhã | Reativação do mesmo pool `/24` no servidor do dashboard reproduz F04 | `--- Configuração do daemon Docker (daemon.json) ---`<br>`{ "bip": "192.168.4.1/24", "default-address-pools": [ { "base": "192.168.4.0/24", "size": 24 } ], ... }` | Mesma config restrita do servidor RPA replicada na VM do dashboard pelo time do parceiro (padrão de provisionamento) | Reuso do fix de F04: expandir pool pra `172.16.0.0/12 size 24` no `daemon.json` e reiniciar Docker | `check-docker` (mesmo de F04) | [Turno 27/03 Manhã](registro interno) |
| F18 | 27/03/2026 Manhã | Container `dashboard` falha na sincronização porque `RPA_DATABASE_URL` no `.env` está malformada | `RPA_DATABASE_URL=siscandashboard` (esperado: `postgresql://user:pass@host:5432/db`) | Preenchimento manual da variável apenas com o nome do banco — script de setup não validava formato | Corrigir para `postgresql://siscanrpa:<senha>@198.51.100.20:5432/siscanrpa` | `check-env` (categoria *Formato e domínio* → `RPA_DATABASE_URL formato postgresql:// válido`) — corresponde ao Problema 8 do TROUBLESHOOTING | [Turno 27/03 Manhã](registro interno) |
| F19 | 27/03/2026 Tarde | `backup_manager.sh` aborta porque a porta 80 está em uso e o helper detecta antes do exec | `Container 'app' não está em execução.`<br>`Aviso: a porta 80 já está em uso por um processo local.`<br>`Pare o processo antes de iniciar a stack Docker (ex.: python run.py).`<br>`Para identificar o processo:`<br>`  ss -tlnp | grep :80` | `_app_is_running` do script genérico testava porta 80 (assumindo dev), mas a stack de produção subia na porta 5001; container `app` estava up e healthy, falso negativo | Workaround: invocar diretamente `docker compose -f docker-compose.prd.rpa.yml exec ...`. Fix consolidado em PR #533 do `siscan-rpa` (corrige conflito de porta no helper) | `check-stack` (categoria *Portas externas — porta livre*) — corresponde ao Problema 11 do TROUBLESHOOTING | [Turno 27/03 Tarde](registro interno) |
| F20 | 27/03/2026 Tarde | `pg_restore` aborta com parâmetro desconhecido por dump vir de servidor mais novo | `pg_restore: error: could not execute query: ERROR:  unrecognized configuration parameter "transaction_timeout"`<br>`Command was: SET transaction_timeout = 0;` | Dump gerado com `pg_dump` v17 (PostgreSQL 17) escreve `SET transaction_timeout = 0;`; servidor parceiro roda versão mais antiga que não conhece esse parâmetro | Pipe com `grep -v transaction_timeout` antes do `psql` (workaround) — `pg_restore ... --file=- ... | grep -v transaction_timeout | psql ...` | `check-db` (avisa quando `psql`/`pg_isready` ausentes) — corresponde ao Problema 5 do TROUBLESHOOTING | [Turno 27/03 Tarde](registro interno) |
| F21 | 27/03/2026 Tarde | `pg_restore` recusa redirecionar para `/dev/stdout` por permissão | `/usr/lib/postgresql/17/bin/pg_restore: option requires an argument -- 'f'`<br>`pg_restore: hint: Try "pg_restore --help" for more information.`<br>`bash: line 2: /dev/stdout: Permission denied` | Quebra de linha no comando + `bash -c "pg_restore ... -f /dev/stdout"` rodando como usuário não-root dentro do container negou escrita em `/dev/stdout` | Substituir por `--file=-` (forma POSIX padrão pra stdout) — `pg_restore --file=- --clean --if-exists --no-owner` | manual (workaround pontual do restore — não recorrente) | [Turno 27/03 Tarde](registro interno) |
| F22 | 27/03/2026 Tarde | Comando `pg_restore` digitado sem prefixo `docker compose -f` cai como nome de arquivo | `siscan@<HOST-RPA>:/app/assistente-siscan-rpa$ docker-compose.prd.rpa.yml exec -T ... pg_restore ...`<br>`-bash: docker-compose.prd.rpa.yml: command not found` | Erro de digitação durante a sessão ao vivo (faltou `docker compose -f` no início da linha) | Re-emitir o comando com `docker compose -f docker-compose.prd.rpa.yml exec ...` | manual (erro de digitação — não cobertura técnica) | [Turno 27/03 Tarde](registro interno) |
| F23 | 01/04/2026 Manhã | "Senhas expirando" — autenticação dos usuários quebra após cada deploy | (sintoma observado em F14, agora diagnosticado) | **Causa raiz definitiva**: chaves RSA (`rsa_private_key.pem` / `rsa_public_key.pem`) não estavam em volume persistente — `docker compose up -d` recriava containers e regenerava chaves a cada deploy, invalidando todos os hashes de senha no banco | PR #540 no `siscan-rpa`: persistir chaves RSA em volume Docker. PR #542: migrar senhas de usuário de RSA para bcrypt (defesa em profundidade — separa "criptografia de senhas SISCAN" de "hash de senha de login") | `check-permissions` (escopo futuro #43 — alinhar `HOST_SECRETS_DIR` com workflows CD); corresponde ao Problema 10 do TROUBLESHOOTING | [Turno 01/04 Manhã](registro interno) |
| F24 | 01/04/2026 Manhã | `cli.py list-users` invocado mas comando não existe | `Usage: cli.py [OPTIONS] COMMAND [ARGS]...`<br>`Try 'cli.py --help' for help.`<br>`No such command 'list-users'.` | Documentação operacional citava comando que nunca existiu no `cli.py` (referência operacional sobre versão antiga) | Usar `set-user-password <usuario>` diretamente (sem listagem); listagem feita via SQL no banco | manual (ação CLI da aplicação `siscan-rpa`) | [Turno 01/04 Manhã](registro interno) |
| F25 | 01/04/2026 Manhã | `set-user-password` chamado com sintaxe errada de flag | `Usage: cli.py set-user-password [OPTIONS] USERNAME`<br>`Try 'cli.py set-user-password --help' for help.`<br>`No such option: --username` | Sintaxe errada da CLI — username é argumento posicional, não flag `--username` | `docker exec -it siscan-rpa-app-1 python cli.py set-user-password adminsms` (password solicitado interativamente) | manual (ação CLI da aplicação `siscan-rpa`) | [Turno 01/04 Manhã](registro interno) |
| F26 | 01/04/2026 Tarde | Download de PDF do laudo falha com `PermissionError` no diretório de auth do Playwright | `Exceção PermissionError`<br>`Módulo src.clients.executar_rpa_download_pdf`<br>`Mensagem [Errno 13] Permission denied: '/app/data/.artifacts/auth'` | UID 1000 do `appuser` dentro do container não tinha permissão para criar/escrever em `./data/.artifacts/auth` no bind mount — diretório criado no host pertencia ao root | `mkdir -p ./data/.artifacts/auth && chown -R 1000:1000 ./data/.artifacts && chmod -R 755 ./data/.artifacts`. Fix definitivo: PR #544 (cria diretório no entrypoint) + PR #546 (chown recursivo nos bind mounts) no `siscan-rpa` | `check-permissions` (categoria *Diretórios HOST_*: owner + escrevível*) — corresponde ao Problema 10 do TROUBLESHOOTING | [Turno 01/04 Tarde](registro interno) |
| F27 | 06/05/2026 Manhã | Runner `siscan-dashboard` cai com erro de SSL após whitelist expirar silenciosamente | `Apr 30 23:59:50 ... runsvc.sh[...]: 2026-05-01 02:59:50Z: Runner connect error: The SSL connection could not be established, see inner exception.. Retrying until reconnected.`<br>`May 01 00:10:59 ... runsvc.sh[...]: An error occurred: The SSL connection could not be established, see inner exception.`<br>`May 01 00:10:59 ... runsvc.sh[...]: Runner listener exited with error code 2` | Whitelist de FW aplicada em 19/03 tinha prazo de expiração não comunicado; em 30/04/01 a regra caiu e o runner voltou ao estado de F01/F02 | Validação com `curl -v https://api.github.com` (reproduz `SSL_ERROR_SYSCALL`). E-mail formal com whitelist por FQDN + nota sobre DPI/SSL inspection enviado pra equipe de FW → reaberta em 11/05 | `check-network` (22 endpoints categorizados em `network-endpoints.json`) — corresponde ao Problema 3 do TROUBLESHOOTING | [Turno 06/05 Manhã](registro interno) |
| F28 | 06/05/2026 Manhã | `timedatectl set-ntp true` falha no Ubuntu Server | `Failed to set ntp: NTP not supported` | VM sem `systemd-timesyncd` instalado/habilitado; clock sincronizado por outro mecanismo (vmware tools / chrony) | Ignorar — `timedatectl status` confirma `System clock synchronized: yes` (mecanismo alternativo cobrindo a função) | `check-deps` (categoria *Sincronização de tempo*) detecta `clock sincronizado` independente do mecanismo | [Turno 06/05 Manhã](registro interno) |
| F29 | 25/05/2026 Manhã | Job CD fica em "Waiting for a runner" indefinidamente mesmo com runner `active (running)` e firewall reaberto | (sintoma — log do runner mostra `Listening for Jobs` mas job nunca despacha; após 14+ min sem pegar) | **Regra dos 30 dias do GitHub**: `If you do not perform a software update within 30 days, the GitHub Actions service will not queue jobs to your runner.` Runner ficou 37 dias offline (firewall caído 30/04 → 25/05), perdeu pelo menos uma versão | `sudo systemctl stop 'actions.runner.*' && cd ~siscan/actions-runner && sudo -u siscan ./run.sh --check` (força auto-update) → re-registrar com token novo | `check-runner` (categoria *Idade do runner — regra dos 30 dias*) → automatizado por `siscan-runner-recover.sh` — corresponde ao Problema 7 do TROUBLESHOOTING | [Turno 25/05 Manhã](registro interno) |
| F30 | 25/05/2026 Manhã | `sudo -u runner ./run.sh --check` falha porque usuário `runner` não existe nessa VM | `sudo: unknown user runner`<br>`sudo: error initializing audit plugin sudoers_audit` | Documentação genérica do GitHub usa `runner` como usuário convencional; nas VMs do projeto, o usuário é `siscan` (definido pelo `siscan-server-setup.sh`) | `sudo -u siscan ./run.sh --check` | `check-runner` (validação do user dono dos arquivos `actions-runner/`) | [Turno 25/05 Manhã](registro interno) |
| F31 | 25/05/2026 Tarde | Primeira execução do `siscan-server-doctor.sh` retorna 1/9 OK · 8 FAIL | `"specialist": "check-env" ... "specialist saiu com exit=2 sem JSON válido (provável .env/PRODUCTS_FILE ausente)"` (e mesmo padrão em 7 outros specialists) | `jq` não instalado na VM — todos os specialists que parseiam `products.json` falham no pré-requisito | `sudo apt install -y jq` → re-rodar doctor (sobe pra 6/9 OK) | `check-deps` (categoria *Network tools — jq*) — após o fix passa a ser detectado pré-emptivamente | [Turno 25/05 Tarde](registro interno) |
| F32 | 25/05/2026 Tarde | Doctor identifica 2 endpoints GitHub ainda bloqueados após reabertura parcial do FW | `✘  pipelines.actions.githubusercontent.com   firewall bloqueou — sem resposta (TCP/TLS não completou)`<br>`✘  productionresultssa0.blob.core.windows.net   firewall bloqueou — sem resposta (TCP/TLS não completou)` | Whitelist reaberta pelo time de segurança não incluiu os 2 endpoints específicos do Actions: pickup de jobs / OIDC (`pipelines...`) e Azure Blob para logs/artifacts (`productionresultssa0...`) | Requisição interna 767679 — Liberação para o GITHUB - Servidores SISCAN aberta com lista exata dos 2 FAILs + relatório técnico v2.0 "Reativação de whitelist" | `check-network` (categoria *Runner ↔️ GitHub Actions (HTTPS/443)*) | [Turno 25/05 Tarde](registro interno) |
| F33 | 25/05/2026 Tarde | RAM da VM `siscan-dashboard` abaixo do mínimo declarado no `DEPLOY_SERVER.md` | `✘  <HOST-DASHBOARD>   7.7 GB (esperado >= 8 GB) — pode causar OOM kills sob carga` | VM provisionada com 8 GB nominais mas o kernel/hypervisor consome ~300 MB; resta 7.7 GB visíveis ao SO | Aumento solicitado para 8-10 GB nominais (margem operacional); operação atual segue sem OOM enquanto carga ficar moderada | `check-resources` (categoria *Memória RAM*) | [Turno 25/05 Tarde](registro interno) |
| F34 | 26/05/2026 Manhã | Doctor reporta serviço systemd do runner ausente, mas arquivos locais OK | `✘  actions.runner.*   nenhum serviço encontrado — rode sudo $RUNNER_DIR/svc.sh install` | Recover do dia 25/05 fez `svc.sh stop` mas não reinstalou — unit systemd ficou removida do disco; `config.sh`/`.runner` intactos | `cd ~/actions-runner && sudo ./svc.sh install siscan && sudo ./svc.sh start` (Cenário C entregue em PR #52 — flag `--token` no `siscan-runner-recover.sh`) | `check-runner` (categoria *Serviço systemd*) → automatizado pelo Cenário C do `siscan-runner-recover.sh` | [Turno 26/05 Manhã](registro interno) |
| F35 | 27/05/2026 Manhã | Cenário A/A2 do recover trava com `.runner` órfão após `config.sh remove` silenciosamente falho | `# Runner removal`<br>`⚠  config remove (404 esperado se runner já foi auto-removido)`<br>(em seguida, ao registrar:)<br>`Cannot configure the runner because it is already configured. To reconfigure the runner, run 'config.cmd remove' or './config.sh remove' first.`<br>`Falha ao registrar o runner. Verifique URL e token (tokens expiram em poucos minutos).` | `runner_remove_registration` (em `scripts/deploy_server/_runner.sh`) passava o **registration-token** para `./config.sh remove`, que precisa de **remove-token** (endpoint distinto). A falha era engolida por `2>/dev/null`; o `.runner` permanecia em disco; o `./config.sh` subsequente abortava com "already configured" mesmo passando `--replace` | Workaround imediato: `rm -f ~/actions-runner/.runner ~/actions-runner/.credentials ~/actions-runner/.credentials_rsaparams` antes do recover (queda em estado 2 — bootstrap incremental sem `config.sh remove`). Fix permanente em [issue #63](https://github.com/Prisma-Consultoria/assistente-siscan-rpa/issues/63): estratégia em 3 camadas (detecção remota + remove-token via API + fallback local determinístico) | `runner_remove_registration` (cobertura via testes bats em `tests/unit/test_runner_remove_registration.bats`) | [Turno 27/05 Manhã](registro interno) |
| F36 | 27/05/2026 Manhã | Recover em estado 3 (sem systemd) caía sempre em Cenário C, mesmo quando runner já tinha sido auto-removido pelo GitHub | `→  → Cenário C: systemd unit ausente, .runner válido — install + start (sem token)`<br>(serviço sobe local, mas runner não pega jobs — heartbeat 401 em loop com credencial inválida) | `runner_diagnose` em `_runner.sh` mapeava `state=3` diretamente para Cenário C sem consultar a API. Quando o `.runner` local persistia após auto-remoção remota (>14d offline), o caminho C presumia incorretamente que o registro local era válido | Workaround manual: `svc.sh stop && svc.sh uninstall && rm -f .runner .credentials .credentials_rsaparams && bash siscan-runner-recover.sh --token <REG-TOKEN>` (força queda em estado 2 — re-registro completo). Fix permanente em [issue #65](https://github.com/Prisma-Consultoria/assistente-siscan-rpa/issues/65): `runner_diagnose` em state=3 consulta API; se `total_count: 0`, redireciona para A2 (re-registro defensivo) | `runner_diagnose` (cobertura via testes bats em `tests/unit/test_runner_diagnose_state3.bats`) | [Turno 27/05 Manhã](registro interno) |
| F37 | 27/05/2026 Manhã | Specialist `check-runner` reporta `4/4 OK` mesmo quando o check de registro remoto foi silenciosamente pulado por falta de credencial | `=== Registro remoto no GitHub ===`<br>`⚠  gh CLI ausente e GH_TOKEN não setado — pulando check remoto`<br>`=== Resumo (check-runner) ===`<br>`  4/4 OK` ← falso-positivo: o specialist só executou 3/4 checks | `check-runner` usava `warn` solto pra registrar pulo do check remoto. O helper `warn` imprime na tela mas **não contabiliza** no resumo (`TOTAL`/`OK_COUNT`/`FAIL_COUNT`). Operador via "verde total" e confiava — mas o check mais crítico (registro remoto) não havia rodado | Fix permanente em [issue #66](https://github.com/Prisma-Consultoria/assistente-siscan-rpa/issues/66): novo helper `add_skipped` em `_common.sh` (análogo a `add_ok`/`add_fail`) com contador `SKIPPED_COUNT`. Resumo passa a mostrar `<ok>/<total> OK · <n> SKIPPED`. `check-runner` usa `add_skipped` nos 3 pontos onde antes pulava silenciosamente | `add_skipped` em `_common.sh` (cobertura via `tests/unit/test_add_skipped.bats`); `check-runner` revisado para SKIPPED no bloco "Registro remoto no GitHub" | [Turno 27/05 Manhã](registro interno) |
| F38 | 28/05/2026 Manhã | Recover repete o mesmo `SSL connection could not be established` mesmo após `installdependencies.sh` atualizar `libssl3` e `check-network` reportar 4/4 OK | (no terminal:)<br>`# Authentication`<br>`The SSL connection could not be established, see inner exception.`<br>(no `~/actions-runner/_diag/Runner_*.log`:)<br>`GET request to https://pipelinesghubeus6.actions.githubusercontent.com/<TOKEN>/_apis/connectionData?... failed.`<br>`System.IO.IOException: Received an unexpected EOF or 0 bytes from the transport stream.`<br>`at System.Net.Security.SslStream.ReceiveHandshakeFrameAsync` | Whitelist do firewall corporativo (req 767679, 26/05 15:15) cobria apenas o endpoint **base** `pipelines.actions.githubusercontent.com` (sem o prefixo regional `ghubeus6`). O backend do GitHub Actions roteia dinamicamente para variantes regionais — neste caso a VM roteava para `pipelinesghubeus6.actions.githubusercontent.com`, bloqueada pelo firewall. Peer fechava conexão durante TLS handshake (TCP RST), gerando `Received unexpected EOF` no .NET — distinto de CA bundle (que apareceria como `AuthenticationException`). `check-network` não detectava porque testava só o endpoint base. | Workaround imediato: solicitar liberação **wildcard** `*.actions.githubusercontent.com` ao time de segurança (cobre todas as variantes regionais dinâmicas). Fixes em código: (a) `runner_diagnose_tls_failure` em [issue #82](https://github.com/Prisma-Consultoria/assistente-siscan-rpa/issues/82) emite o stack trace + extrai o URL específico do log + recomenda wildcard automaticamente quando o recover falhar; (b) `network-endpoints.json` em [issue #84](https://github.com/Prisma-Consultoria/assistente-siscan-rpa/issues/84) tem guidance.on_any_fail atualizada para explicitar wildcard como recomendação primária no pedido à TI; (c) [issue #87](https://github.com/Prisma-Consultoria/assistente-siscan-rpa/issues/87) estende `runner_diagnose_tls_failure` para **resolver o host extraído em DNS** (seção [4]) e **testar alcance direto via curl** ao endpoint específico (seção [6]) — `HTTP=000` confirma firewall, `HTTP=2xx/4xx` indica TLS subiu e revela outra causa | `runner_diagnose_tls_failure` em `_runner.sh` — 6 seções emitidas em stderr na failure path de `runner_register` (cobertura via `tests/unit/test_runner_diagnose_tls_failure.bats` — 37 testes); `check-network` (categoria *Runner ↔️ GitHub Actions*) com guidance.on_any_fail mencionando wildcard + categoria advisory `runner_actions_regional_variants` com 12 variantes regionais conhecidas (TSK00.04.09 #88); endpoints com `wildcard_for` ganham campo `warning_when_alone` que emite aviso pós-OK orientando operador (TSK00.04.11 #90); flag `--advisory-strict` no specialist para upgrade de advisory a FAIL bloqueante em CI/automação estrita (idem). **Fundamentação oficial**: documentação oficial GitHub (https://docs.github.com/en/actions/reference/runners/self-hosted-runners#communication, seção *Accessible domains by function*) recomenda wildcards explicitamente — não é só achado empírico nosso; o pattern aplica-se também a `*.blob.core.windows.net` e `*.pkg.github.com`. | [Turno 28/05 Manhã](registro interno) — lab #259 |

> Síntese: 38 incidentes documentados — 22 em março (oficinas #1–#3 + cascade de fixes),
> 4 em 01/04 (cascade RSA + UID 1000), 12 em maio (firewall expirado + maratona
> doctor/recover de 25–27/05 + variantes regionais reveladas em 28/05). Cobertura
> automatizada hoje: 32/38 detectáveis por specialists ou tratados pelo
> `siscan-runner-recover.sh` (F38 é detectado/diagnosticado automaticamente pelo
> `runner_diagnose_tls_failure` no recover, mas a **remediação operacional**
> exige liberação no firewall corporativo); 6/38 permanecem manuais (ações em
> CLI da aplicação ou no GitHub UI).

---

## Erros comuns — ambos os modos

| # | Erro (mensagem comum) | Causa provável | Solução prática |
|---:|---|---|---|
| 1 | `Cannot connect to the Docker daemon` | Docker daemon parado ou sem permissão | Iniciar o Docker; adicionar usuário ao grupo `docker`; executar como Admin/root |
| 2 | `pull access denied` / `unauthorized` | Token GHCR inválido, expirado ou sem `read:packages` | Regenerar PAT; `docker logout ghcr.io` e novo login. Ver [TROUBLESHOOTING.md — Problema A](TROUBLESHOOTING.md) |
| 3 | `mount denied: path not found` | Caminho `HOST_*` inexistente no `.env` | Criar a pasta; verificar variável no `.env` |
| 4 | `Bind: address already in use` | Porta do host em uso | Identificar o processo (`ss -ltnp` / `netstat -ano`) e parar, ou alterar `HOST_APP_EXTERNAL_PORT` |
| 5 | `no space left on device` | Disco cheio | Limpar logs antigos; `docker system prune`; expandir disco |
| 6 | `TLS handshake timeout` | Proxy / inspeção TLS / latência | Verificar configuração de proxy; importar CA corporativa |
| 7 | `network not found` | Rede do compose ausente | `docker network create <name>` ou verificar nome da rede no compose |
| 8 | `container is unhealthy` | Healthcheck falhou | `docker compose logs app --tail=50`; verificar banco e variáveis |
| 9 | `CrashLoop / restart policy` | Exceção na aplicação ao iniciar | `docker compose logs` para identificar a causa raiz; verificar `.env` e banco |
| 10 | `docker compose config returns error` | YAML inválido | Corrigir sintaxe (indentação, variáveis não resolvidas) |
| 11 | `Error pulling image: manifest unknown` | Tag inexistente no GHCR | A tag correta é `main`; verificar `docker pull ghcr.io/prisma-consultoria/siscan-rpa-rpa:main` |
| 12 | `invalid mount config for type "bind"` | Variável `HOST_*` vazia ou caminho inválido | Verificar `.env`; garantir caminhos absolutos e pastas existentes |
| 13 | `ghcr.io resolve name temporarily unavailable` | Falha de DNS | `nslookup ghcr.io`; ajustar DNS ou contatar NetOps |
| 14 | `504 Gateway Timeout` no pull | Proxy filtrando ou latência alta | Ver logs do proxy; solicitar bypass |
| 15 | `Healthcheck failing intermittently` | Recursos escassos ou latência | Verificar uso de CPU/memória; escalar recursos se necessário |
| 16 | `Rate limit errors` | Pulls sem autenticação | Fazer `docker login ghcr.io` antes do pull |
| 17 | `Image pull stuck at 0%` | Problema de rede/DNS | `curl https://ghcr.io` para testar conectividade |
| 18 | `Error response from daemon: conflict: unable to delete` | Recurso em uso | Parar o container antes de remover a imagem |
| 19 | `Compose up hangs on creating volume` | Permissão ou driver de volume | Verificar ACLs; testar `docker volume create` manualmente |
| 20 | `DNS blocked by firewall` | Firewall corporativo bloqueando DNS | Solicitar liberação de `ghcr.io` porta 443 ao time de NetOps |
| 21 | `Unknown shorthand flag: 'd' in -d` | Docker Compose v1 desatualizado | Atualizar para Compose v2: `docker compose version` deve retornar `v2.x.x` |
| 22 | `no such image: image-name` | Tag ausente localmente e pull falhou | `docker pull ghcr.io/prisma-consultoria/siscan-rpa-rpa:main` manualmente |

---

## Erros específicos — Modo HOST (Windows / Docker Desktop)

| # | Erro (mensagem comum) | Causa provável | Solução prática |
|---:|---|---|---|
| 23 | `.\siscan-assistente.ps1 não pode ser carregado` | ExecutionPolicy bloqueando scripts PowerShell | `Set-ExecutionPolicy RemoteSigned -Scope LocalMachine` ou usar `execute.ps1`. Ver [TROUBLESHOOTING.md — Problema 1](TROUBLESHOOTING.md) |
| 24 | `PermissionError: [Errno 13] Permission denied` em volume | Docker Desktop/WSL2 não permite criar subpastas em bind mount | Criar diretórios no Windows antes de subir o container. Ver [TROUBLESHOOTING.md — Problema 2](TROUBLESHOOTING.md) |
| 25 | `is not valid windows path` / `invalid mount config` | Caminho UNC com caracteres especiais (`&`, `%`, etc.) | Mapear unidade de rede (`net use`) ou renomear a pasta. Ver [TROUBLESHOOTING.md — Problema 3](TROUBLESHOOTING.md) |
| 26 | `Permission denied on named pipe \\.\pipe\docker_engine` | Acesso ao pipe do Docker negado | Executar Docker Desktop como Administrador; verificar grupo `docker-users` |
| 27 | `Windows update broke docker` | Kernel / WSL2 desatualizado após update do Windows | `wsl --update`; reinstalar Docker Desktop se necessário |
| 28 | `file path too long` | Path Windows > 260 caracteres | Habilitar long paths: `Set-ItemProperty -Path "HKLM:\SYSTEM\CurrentControlSet\Control\FileSystem" -Name LongPathsEnabled -Value 1` |
| 29 | `Docker Desktop cannot be installed on this edition` | Edição do Windows sem Hyper-V | Habilitar WSL2; verificar requisitos de hardware e edição do Windows |
| 30 | `Not enough memory to allocate` | Limites de recursos no Docker Desktop | Docker Desktop → Settings → Resources → aumentar memória/CPU alocados |
| 31 | `Credential helper not working` | `credHelpers` mal configurado em `~/.docker/config.json` | Remover entrada de `credHelpers`; testar `docker login ghcr.io` diretamente |

---

## Erros específicos — Modo Servidor (Ubuntu / PostgreSQL externo)

| # | Erro (mensagem comum) | Causa provável | Solução prática |
|---:|---|---|---|
| 32 | `could not connect to server: Connection refused` | `DATABASE_HOST` errado ou PostgreSQL inacessível | Verificar IP/hostname; `nc -zv $DATABASE_HOST 5432`; verificar firewall. Ver [TROUBLESHOOTING.md — Problema 1](TROUBLESHOOTING.md) |
| 33 | `FATAL: password authentication failed` | `DATABASE_PASSWORD` incorreto ou usuário não existe no banco | Verificar credenciais no `.env`; confirmar usuário no PostgreSQL externo |
| 34 | `FATAL: no pg_hba.conf entry for host` | PostgreSQL não permite conexão do IP do servidor de app | Adicionar regra no `pg_hba.conf`; reiniciar o PostgreSQL |
| 35 | Runner GitHub Actions `Offline` | Serviço do runner parado ou sem acesso a `github.com` | `cd ~/actions-runner && ./svc.sh start`; verificar porta 443 para `github.com`. Ver [TROUBLESHOOTING.md — Problema 2](TROUBLESHOOTING.md) |
| 36 | `PermissionError` em diretórios Linux bind-mounted | UID do container diferente do dono das pastas no host | `chown -R <UID> /opt/siscan-rpa/logs` (e demais pastas `HOST_*`). Ver [TROUBLESHOOTING.md — Problema 3](TROUBLESHOOTING.md) |
| 37 | `Cannot connect to the Docker daemon at unix:///var/run/docker.sock` | Usuário não está no grupo `docker` | `sudo usermod -aG docker $USER` + logout/login (fase 7 do `siscan-server-setup.sh` faz isso) |

---

> Adaptação das soluções conforme políticas da organização (mirrors internos, proxies autenticados, agentes de deploy) é recomendada.
