###########################################################################
# Copie este arquivo para `.env` e preencha os valores HOST_* com
# caminhos absolutos do host que o Docker Desktop consegue montar.
# Exemplos de caminhos válidos no Windows (Docker Desktop WSL2):
#  - PowerShell: C:\Users\cliente\siscan\media
#  - WSL: /mnt/c/Users/cliente/siscan/media
# Exemplos de caminhos válidos no Linux:
#  - /home/cliente/siscan/media
#  - /var/siscan/media
# Use o formato que funciona no seu ambiente Docker.
###########################################################################

######################### 1) CONFIGURAÇÃO DO APLICATIVO #########################
# Porta externa que o usuário acessa (ex.: 5001). Se houver proxy, ajuste.
HOST_APP_EXTERNAL_PORT=5001

######################### 2) CREDENCIAIS SISCAN (OBRIGATÓRIO) #################
# IMPORTANTE (não técnico): preencha com o usuário e senha fornecidos pelo SISCAN.
# Sem esses valores, a automação não conseguirá acessar o sistema.
SISCAN_USER=
SISCAN_PASSWORD=


######################### 3) DIRETÓRIOS (HOST) ################################
# Estes caminhos devem existir no Windows e serem acessíveis pelo Docker.
# Para usuários não técnicos: crie as pastas no Windows exatamente como abaixo
# (por exemplo usando o Explorer) ou peça ao time de infra.

# Diretório raiz de mídia (OBRIGATÓRIO)
# Deve conter a seguinte estrutura de subdiretórios:
#   HOST_MEDIA_ROOT/
#     ├── downloads/               (DOWNLOAD_DIR, SISCAN_REPORTS_INPUT_DIR)
#     │   └── [PASTA-PERIODO]/
#     │       └── extracao_laudo/   (REPORT_EXTRACTOR_DIR_NAME)
#     └── reports/
#         └── mamografia/          (SISCAN_REPORTS_OUTPUT_DIR)
#             └── consolidado/
#                 ├── monitoramento_mamografia.xlsx
#                 └── exames/          (PDFs consolidados)
#
# Exemplo Windows: C:\siscan\media
# Exemplo Linux: /var/siscan/media
HOST_MEDIA_ROOT=./media

# Nome do arquivo Excel consolidado (padrão)
# Será salvo em: HOST_MEDIA_ROOT/reports/mamografia/consolidado/
HOST_CONSOLIDATED_EXCEL_FILENAME=monitoramento_mamografia.xlsx

# Diretório de configuração (contém arquivo de mapeamento de colunas)
# Exemplo Windows: C:\siscan\config
# Exemplo Linux: /var/siscan/config
HOST_EXCEL_COLUMNS_MAPPING_DIR=./config

# Nome do arquivo de mapeamento de colunas do Excel
# Será buscado em: HOST_EXCEL_COLUMNS_MAPPING_DIR/
HOST_EXCEL_COLUMNS_MAPPING_FILENAME=excel_columns_mapping.json


######################### 4) AGENDAMENTO / CRON ###############################
# Habilita o agendamento de tarefas periódicas (true/false)
CRON_ENABLED=true
# Intervalo em segundos entre execuções agendadas (ex.: 1800 = 30 minutos)
CRON_INTERVAL_SECONDS=1800

######################### 5) RETRIES / TENTATIVAS #############################
# Número máximo de tentativas quando algo falha (ex.: falha de rede)
RPA_MAX_ATTEMPTS=3

######################### 6) INSTRUÇÕES RÁPIDAS PARA NÃO TÉCNICOS #############
# - Copie este arquivo para `.env`.
# - Preencha apenas as chaves destacadas como OBRIGATÓRIO (ex.: SISCAN_USER).
# - Se não souber os caminhos HOST_*, solicite ao time de infraestrutura.
# - Para testar localmente no Windows com Docker Desktop, use caminhos no formato
#   `C:\caminho\para\pasta` ou `/mnt/c/...` se estiver usando WSL conforme o seu setup.

# Se precisar, eu posso gerar um `.env` de exemplo já preenchido com caminhos
# padrão (ex.: `C:\siscan\media`) — me avise se quer que eu faça isso.

###########################################################################
